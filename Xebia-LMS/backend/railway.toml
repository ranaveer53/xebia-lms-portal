[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/api/actuator/health"
healthcheckTimeout = 300
startDetectionTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
